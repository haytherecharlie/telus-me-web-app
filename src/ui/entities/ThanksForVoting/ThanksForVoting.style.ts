import styled from 'styled-components'

export const ThanksForVoting = styled('div')({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center'
})

export const Image = styled('img')({
  width: `90%`,
  maxWidth: 500,
  margin: `100px 0 50px 0`
})
