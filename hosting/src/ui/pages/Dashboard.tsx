import React, { useEffect } from 'react'
import PageLayout from 'ui/layouts/PageLayout'

const Results = () => {
  useEffect(() => {}, [])

  return (
    <PageLayout page="404" crawl={false} style={{ alignItems: 'center', justifyContent: 'flex-start' }}></PageLayout>
  )
}

export default Results
